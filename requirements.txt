# Polymarket
py-clob-client>=0.34.6
httpx>=0.27.0

# Solana
solders>=0.21.0
solana>=0.34.0
base58>=2.1.1

# Data + persistence
sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.9   # uncomment if using Postgres instead of SQLite
alembic>=1.13.0
pandas>=2.2.0
numpy>=1.26.0

# Async + scheduling
aiohttp>=3.9.0
APScheduler>=3.10.0
tenacity>=8.2.0

# Config + secrets
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Monitoring + alerts
structlog>=24.1.0
discord-webhook>=1.3.0

# LLM signals (optional, for news_event strategy)
anthropic>=0.40.0

# Backtesting
vectorbt>=0.26.0  # optional, heavy install — comment out if not needed yet

# CLI + dashboard
click>=8.1.0
rich>=13.7.0
streamlit>=1.32.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
mypy>=1.9.0
